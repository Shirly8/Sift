name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *'   # every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -f https://sift-xkk6.onrender.com/api/health-check
